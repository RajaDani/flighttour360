import { Box } from '@mui/material';
import React, { useEffect } from 'react';

const TawkToDashboard = () => {

  return (
<iframe
      src="https://dashboard.tawk.to/login"
      height="500"
      width="800"
      title="Tawk.to Dashboard"
    />
  );
};

export default TawkToDashboard;
